<project>

	<!-- This is NOT a MAVEN file it just uses a similar syntax -->

	<name>${project.name}</name>
	<description>This is the PluginManager. Use it to modify an existing product configuration or create a new product from scratch.</description>
	<version>${project.version}</version>
	<localRepos>${settings.localRepository}</localRepos>

	<workspaces>
	</workspaces>

	<dependencies>
		<dependency>
			<groupId>de.dlr-se.emir.runtime</groupId>
			<artifactId>PluginManagerPlugin</artifactId>
			<version>${runtime.version}</version>
		</dependency>
	</dependencies>

	<repositories>
	</repositories>

</project>